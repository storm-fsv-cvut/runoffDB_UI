{% macro soilsample(key, soilsample, assignment) %}
    <dt>{{ key|trans }}</dt>
    {% if soilsample!=null %}
        <dd>{{ soilsample.getId() }} <i>({{ assignment.getDescriptionCZ() }})</i></dd>
        {#
        <dd><a href="javascript:;" data-toggle="modal" data-target="#modal-{{ key }}">{{ soilsample.getId() }}</a> <i>({{ assignment.getDescriptionCZ() }})</i></dd>
        <div class="modal fade" id="modal-{{ key }}" style="display: none;">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-body">
                        <dl>
                            <dt>{% trans %}processed_at{% endtrans %}:</dt>
                            <dd>{{soilsample.getProcessedAt().name}}</dd>
                        </dl>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default pull-left" data-dismiss="modal">{% trans %}close{% endtrans %}</button>
                    </div>
                </div>
                <!-- /.modal-content -->
            </div>
            <!-- /.modal-dialog -->
        </div>#}
    {% else %}
        <dd>{% trans %}na{% endtrans %}</dd>
    {% endif %}
{% endmacro %}
