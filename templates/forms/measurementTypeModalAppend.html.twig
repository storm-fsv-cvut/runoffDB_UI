<div class="modal fade appendToCollection" id="append_measurement">
    {{ form_start(appendMeasurementForm) }}
    {{ form_widget(appendMeasurementForm.parent_id) }}
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">{% trans %}append measurement{% endtrans %}</h4>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    {{ form_row(appendMeasurementForm.measurementId) }}
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary pull-left" data-dismiss="modal">
                    {% trans %}close{% endtrans %}
                </button>
                {{ form_widget(appendMeasurementForm.save) }}
            </div>
        </div>
        <!-- /.modal-content -->
    </div>
    {{ form_end(appendMeasurementForm,{'render_rest': false}) }}
    <!-- /.modal-dialog -->
</div>
